services:
  rag-server:
    build: .
    container_name: rag-server
    ports:
      - "8000:8000"
    volumes:
      - ./storage:/app/.storage
      - ./models:/app/.models
    env_file:
      - .env
    restart: unless-stopped